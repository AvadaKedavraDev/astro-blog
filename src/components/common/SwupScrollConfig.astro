---
/**
 * @component SwupScrollConfig
 * @description Swup 滚动配置组件（已弃用，功能合并至 SwupCompat）
 * @deprecated 自 2026-03-23 起，此组件的功能已合并到 SwupCompat.astro 中。
 *             保留此组件是为了向后兼容，不再执行任何操作。
 *             滚动配置现在通过 SwupCompat.configureScrollPlugin() 自动管理。
 * 
 * 滚动配置详情：
 * - offset: 96px（导航栏高度 64px + 额外间距 32px）
 * - animateScroll: 根据用户 prefers-reduced-motion 偏好自动设置
 * 
 * 如需自定义滚动配置，请修改 SwupCompat.astro 中的 configureScrollPlugin 调用。
 */
---

<!-- 
  此组件的功能已合并到 SwupCompat.astro 中。
  为了向后兼容，保留此文件，但不再执行任何操作。
  
  滚动配置现在由 SwupCompat 自动管理：
  - 通过 swup.hooks.on('page:view') 配置 Scroll Plugin
  - 自动检测 prefers-reduced-motion 偏好
  - 不再需要轮询查找插件实例
-->
